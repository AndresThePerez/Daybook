# Rate limiting zones
# General API: 10 requests/sec per IP with burst tolerance
limit_req_zone $binary_remote_addr zone=api_general:10m rate=10r/s;

# Write endpoints: 3 requests/sec per IP
limit_req_zone $binary_remote_addr zone=api_write:10m rate=3r/s;

server {
    listen 80;
    server_name _;
    root /var/www/html/public;
    index index.php;

    client_max_body_size 10M;

    # Static assets - serve directly, no rate limit
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # API write operations - strict rate limit
    location ~ ^/api/.*(create|edit|delete) {
        limit_req zone=api_write burst=5 nodelay;
        limit_req_status 429;

        try_files $uri $uri/ /index.php?$query_string;
    }

    # API read operations - general rate limit
    location /api/ {
        limit_req zone=api_general burst=20 nodelay;
        limit_req_status 429;

        try_files $uri $uri/ /index.php?$query_string;
    }

    # All other routes
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
